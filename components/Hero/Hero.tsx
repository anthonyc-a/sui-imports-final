import React from 'react';
import styles from '../../styles/Hero/Hero.module.css';

const Hero: React.FC = () => {
  return (
    <section className={styles.hero}>
      <h1>Welcome</h1>
    </section>
  );
};

export default Hero;
